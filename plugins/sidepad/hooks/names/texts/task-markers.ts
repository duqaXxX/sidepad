/** The box drawn in place of a task list item's `[ ]`. One cell wide: its East Asian width is neutral. */
export const TASK_OPEN_MARKER = '☐';

/** The ticked box drawn in place of a task list item's `[x]`. One cell wide, as the open one. */
export const TASK_DONE_MARKER = '☑';
