/**
 * `/sidepad`'s answer on a terminal too narrow for the pane to be drawn beside the transcript, and
 * the width it names is the engine's own floor for a pane its plugin asked for.
 */
export const RESIZE_TERMINAL_TEXT = 'Resize your terminal to at least 110 columns to show the sidepad pane';
