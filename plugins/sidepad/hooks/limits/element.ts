/**
 * Characters kept past the page's width when a line is cut for one `Code`: the page truncates each
 * line at its right edge, so a line cut past that shows the same, and a few cells of slack cover a
 * tab, which takes one character and several cells.
 */
export const CUT_MARGIN_CHARS = 8;
