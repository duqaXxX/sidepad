/** How often a drag held past the code window's edge scrolls one line. */
export const EDGE_SCROLL_MS = 60;

/** How often a Markdown block's Client checks its laid-out height, known only after a layout. */
export const BLOCK_ROWS_POLL_MS = 200;
